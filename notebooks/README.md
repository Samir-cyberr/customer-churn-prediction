# Notebooks

This directory is reserved for exploratory analysis notebooks.

The core project is intentionally implemented as reusable Python modules instead of putting the entire workflow inside one notebook.
