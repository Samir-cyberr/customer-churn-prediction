# Data

The dataset is generated locally with:

```bash
python -m src.generate_data
```

The generated CSV is intentionally ignored by Git because it is reproducible synthetic data.

For a production portfolio version, the generated dataset can later be replaced with a properly licensed public dataset.
